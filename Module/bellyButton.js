// import the data
// ensure a server is running (command line python -m http.server)

// d3.json("samples.json").then(function(data){
//     console.log(data);
// });

                    // Print the metadata of person 940

// d3.json("samples.json").then(function(data){
//     firstPerson = data.metadata[0];
//     Object.entries(firstPerson).forEach(([one,two]) => {console.log(one + ': ' + two);});
// });

                    // pring only the keys
// d3.json("samples.json").then(function(data) {
//    [oe,two]) => {console.log(one);})
// });n secondPerson = data.metadata[1];
//     Object.entries(secondPerson).forEach((

                    // print the key value pairs
// d3.json("samples.json").then(function(data) {
//     secondPerson = data.metadata[1];
//     console.log(Object.entries(secondPerson))});

